@page "/counter"
@using System.Globalization;


<MudSelect T="CultureInfo" SelectedValuesChanged="@LanguageChanged">
    @foreach (CultureInfo culture in data!)

    {
        <MudSelectItem Value="culture">@culture.NativeName</MudSelectItem>
    }
    </MudSelect>

<PageTitle>Counter</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">Counter</MudText>

<MudText Class="mb-4">Current count: @currentCount</MudText>

<MudButton Color="Color.Primary" Variant="Variant.Filled" @onclick="IncrementCount">Click me</MudButton>



  @*   <MudSelect T="string" >
    @foreach (string str in data2)
    {
        <MudSelectItem Value="str">@str</MudSelectItem>
    }
</MudSelect> *@

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
